# <需求迭代编号>-<需求主题>需求设计

> 来源：<需求输入路径或说明>
> 日期：YYYY-MM-DD
> 需求项：REQ-001, REQ-002
> 聚合风险：无 / 有，说明
> 设计确认：无需单独确认 / 待确认 / 已确认
> 编码相关未决项：0 / <数量，非 0 时不得进入开发计划>

> **TLDR**: {一句话说明本次需求做什么、涉及哪些模块、核心设计边界是什么}

## 生成规则

- 本文档是当前需求输入的需求设计文档，不是长期模块文档。
- 一个需求输入只生成一个需求设计文档；多个需求项在本文档内分节展开。
- 每个关键结论必须绑定证据、图谱复核结果、代码探查结果或用户确认。
- 低置信度推断进入"未决问题"，不得进入"实现分解建议"和"编码上下文"。
- 无关章节直接省略，不为模板完整而扩写。
- 前端/客户端与后端/服务端技术设计按需写在同一需求项中，不拆成独立实现文档。

---

## 1. 文档信息

| 项 | 内容 |
|----|------|
| 需求迭代编号 | |
| 需求主题 | |
| 需求输入 | |
| 详细设计范围 | |
| 非目标 | |
| 关联需求项 | |
| 设计确认依据 | 需求级别、风险项和用户确认记录 |

---

## 2. 图谱复核摘要

来自 GitNexus / codegraph / grep / glob。只写影响设计正确性的关键事实。

| 复核对象 | 结论 | 证据 | 置信度 |
|------|------|------|------|
| 模块职责 | | | 高/中 |
| 接口签名 | | | 高/中 |
| 数据结构 | | | 高/中 |
| 端到端链路 | 入口到返回/展示是否闭合 | | 高/中 |
| UI 入口 | 不涉及 / | | 高/中 |
| 测试入口 | | | 高/中 |

---

## 3. 需求项总览

| 需求项ID | 需求项名称 | 级别 | 是否涉及 UI | 是否涉及数据 | 是否涉及接口 | 设计章节 | 验证方式 |
|--------|----------|------|-------------|-------------|-------------|----------|----------|
| REQ-001 | | 小/中/大 | 是/否 | 是/否 | 是/否 | 4.1 | |

---

## 4. 需求项详细设计

### 4.1 REQ-001：<需求项名称>

#### 4.1.1 设计范围

| 项 | 内容 |
|----|------|
| 目标 | |
| 非目标 | |
| 变更类型 | 新增 / 修改 / 删除 |
| 涉及模块 | |
| 本次不处理 | |

#### 4.1.2 当前事实

| 类型 | 当前事实 | 证据 |
|------|---------|------|
| 现有入口 | | |
| 现有调用链 | | |
| 现有数据结构 | | |
| 现有约束 | | |
| 不允许改动 | | |

#### 4.1.3 契约设计

**接口契约**

| ID | 名称 | 签名/协议 | 输入约束 | 输出 | 错误处理 | 兼容策略 |
|----|------|-----------|---------|------|----------|---------|
| IF-001 | | | | | | |

**数据契约**

| ID | 字段/表/配置项 | 类型 | 约束 | 默认值 | 迁移/兼容策略 |
|----|---------------|------|------|--------|--------------|
| DATA-001 | | | | | |

**跨层数据与接口契约**

仅在跨层传递数据或行为时保留。

| 字段/行为 | 客户端/前端 | 请求构造与转换 | 通信边界 | 服务端/接收端 | 持久化/缓存 | 返回与展示 | 默认值/兼容 |
|----------|-------------|----------------|----------|---------------|-------------|------------|-------------|
| | | | | | | | |

**运行时约束**

只填写本次需求实际涉及的约束；不涉及的维度删除。涉及但无法确认时进入"未决问题"，不得交给编码阶段自行决定。

| 维度 | 是否涉及 | 设计约束 | 证据/确认 |
|------|---------|---------|----------|
| 线程与并发 | 是/否 | 线程归属、切换点、同步方式、竞态处理 | |
| 缓存 | 是/否 | 缓存键、更新、失效、一致性、容量边界 | |
| 生命周期与资源 | 是/否 | 创建、持有、释放、取消、异常清理 | |
| 性能 | 是/否 | 热路径、阻塞操作、批量、频率或时延约束 | |

#### 4.1.4 端到端关键流程

仅描述跨模块、跨进程、异步/线程、状态错误或回退等容易遗漏的路径。简单单模块流程可省略。

| 顺序 | 路径/真实符号 | 输入 | 处理与转换 | 输出/错误 |
|------|---------------|------|------------|-----------|
| | | | | |

#### 4.1.5 界面契约引用

涉及 UI 时引用 `ui-designer` 输出，不在本文档重新设计 UI。

| 项 | 内容 |
|----|------|
| UI 契约来源 | |
| 页面/组件 | |
| 新增/修改字段 | |
| 交互规则 | |
| 状态与校验 | |
| UI 验收方式 | |

#### 4.1.6 客户端/前端技术设计

仅在本次需求影响客户端或前端时保留。

| 项 | 内容 |
|----|------|
| 目标路径与真实类/组件 | |
| 状态与数据映射 | |
| 请求/响应适配 | |
| 错误与兼容处理 | |
| 编码边界 | |

#### 4.1.7 服务端/后端技术设计

仅在本次需求影响服务端或后端时保留。

| 项 | 内容 |
|----|------|
| 目标路径与真实类/函数 | |
| 业务处理 | |
| 数据与持久化 | |
| 错误与兼容处理 | |
| 编码边界 | |

#### 4.1.8 实现要点

1.
2.

#### 4.1.9 设计决策

中/大需求项或存在实际方案取舍时填写。

| ID | 决策 | 原因 | 备选方案 | 代价 | 证据 | 置信度 |
|----|------|------|---------|------|------|--------|
| DEC-001 | | | | | | 高/中 |

#### 4.1.10 关键编码锚点

不重复完整影响面，只保留编码必须定位的路径、真实符号和用途。

| 路径/真实符号 | 操作 | 用途 | 证据 |
|---------------|------|------|------|
| | 增/删/改 | | |

#### 4.1.11 实现分解建议

这里只说明实现依赖和建议顺序，不生成正式 `TASK-xxx`，不记录状态。正式任务由 `dev-planner` 统一编号。

| 顺序 | 实现内容 | 路径/对象 | 依赖 | 完成条件 |
|------|----------|-----------|------|----------|
| | | | | |

#### 4.1.12 验证建议

| 类型 | 场景 | 期望结果 |
|------|------|----------|
| 单元/集成/UI/手工 | | |

#### 4.1.13 编码上下文

| 项 | 内容 |
|----|------|
| 必读输入 | |
| 允许修改 | |
| 禁止修改 | |
| 编码边界 | |
| 验收标准 | |
| 不能编码的未决问题 | |

#### 4.1.14 未决问题

| ID | 问题 | 阻塞程度 | 确认结论 | 确认人 |
|----|------|---------|---------|--------|
| Q-001 | | 高/中/低 | 待确认 | |

影响编码的问题未确认时，设计状态必须为"待确认"，不得进入开发计划。与本次实现无关的问题移入"非本次范围"。

---

## 5. 跨需求项依赖

| 来源需求项 | 依赖需求项 | 依赖原因 | 对开发顺序的影响 | 对测试的影响 |
|----------|----------|---------|----------------|-------------|
| REQ-001 | REQ-002 | | | |

---

## 6. 聚合风险

聚合风险指多个需求项放在同一次需求输入或同一次迭代中共同交付时，因为共享文件、执行顺序、接口兼容、数据迁移、UI 联动、测试组合或回退策略产生的整体风险。它不改变单个需求项级别，但会影响开发计划、测试范围和评审重点。

| 风险 | 影响 | 应对方式 |
|------|------|---------|
| 共享文件修改冲突 | | |
| 需求项执行顺序依赖 | | |
| 组合测试遗漏 | | |
| 回退或兼容成本上升 | | |

---

## 7. 回写计划

| 文档层级 | 是否回写 | 回写对象 | 回写原因 |
|---------|---------|---------|---------|
| 模块设计 | 是/否 | `projects/<工程>/docs/modules/<module-name>/README.md` | |
| 架构总览 | 是/否 | `docs/overview/architecture.md` | |

---

## 8. 修订记录

| 版本 | 日期 | 变更内容 |
|------|------|---------|
| 1.0 | | 初稿 |
