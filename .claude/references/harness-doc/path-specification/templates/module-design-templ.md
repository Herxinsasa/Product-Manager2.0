# <模块名> 模块设计

> 所属工程：<工程名 / 仓库级>
> 文档路径：<projects/<工程>/docs/modules/<module-name>/README.md>
> 最后更新：YYYY-MM-DD
> 更新来源：<需求设计文档 / 图谱补充 / 人工维护>

## 维护规则

- 本文档记录模块长期事实，不记录一次性需求实现步骤。
- 默认一个模块一个目录，使用 `README.md` 作为入口；文档过大时再按主题拆分子文档。
- 多工程仓库的工程级模块设计放在 `projects/<工程>/docs/modules/<module-name>/README.md`；仓库级公共模块放在 `docs/modules/<module-name>/README.md`。
- 修改前优先参考现有 `docs` 下的模块文档、模块索引和历史需求设计。
- 没有现成模块文档时，基于 GitNexus / codegraph / grep / glob 补充模块描述骨架。
- 只写已确认事实；证据不足的内容进入"待确认问题"，不得补幻想内容。
- 只有稳定模块职责、对外接口、核心数据结构、状态流程、依赖、配置、运行时约束、错误处理或测试关注点变化时才回写。

---

## 1. 模块定位

| 项 | 内容 |
|----|------|
| 模块职责 | |
| 非职责 | |
| 主要调用方 | |
| 主要依赖 | |
| 证据来源 | |

## 2. 文件范围

| 路径 | 作用 | 证据 |
|------|------|------|
| | | |

## 3. 对外接口

只记录对其他模块、工程、页面、服务或测试入口稳定可见的接口。

| 接口 | 调用方 | 输入 | 输出 | 错误处理 | 兼容约束 | 证据 |
|------|--------|------|------|----------|----------|------|
| | | | | | | |

## 4. 核心数据结构

| 名称 | 字段/含义 | 生命周期 | 持久化 | 兼容要求 | 证据 |
|------|-----------|----------|--------|----------|------|
| | | | | | |

## 5. 核心流程

描述模块长期稳定的关键流程、状态流转、调用顺序。没有证据时写"待确认"，不要补全。

| 流程 | 触发 | 关键步骤 | 失败处理 | 证据 |
|------|------|----------|----------|------|
| | | | | |

## 6. 依赖关系

| 依赖对象 | 依赖方式 | 用途 | 风险 | 证据 |
|----------|----------|------|------|------|
| | 接口调用 / 消息 / 配置 / 数据 | | | |

## 7. 配置、运行时约束与错误处理

| 类型 | 名称 | 说明 | 默认值/错误码 | 证据 |
|------|------|------|---------------|------|
| | | | | |

运行时约束仅记录模块长期稳定事实，例如线程归属、并发模型、缓存一致性和资源生命周期；一次性实现细节留在需求设计文档。

## 8. 测试关注点

| 场景 | 验证点 | 建议方式 | 证据 |
|------|--------|----------|------|
| | | | |

## 9. 待确认问题

| ID | 问题 | 影响 | 阻塞级别 | 需要谁确认 |
|----|------|------|----------|------------|
| N/A | N/A | N/A | N/A | N/A |

## 10. 修订记录

| 日期 | 来源需求/证据 | 变更摘要 |
|------|---------------|----------|
| | | |
